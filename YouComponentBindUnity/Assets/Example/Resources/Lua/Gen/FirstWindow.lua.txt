-- Generated once by YouBindCollector. This file will not be overwritten.
function FirstWindow_Init(root)
    local view = FirstWindow_InitializeView(root)
    print("[FirstWindow] Init done.")
    if view ~= nil then
        for key, value in pairs(view) do
            print("[FirstWindow] " .. key, value)
        end
    end
    return view
end
