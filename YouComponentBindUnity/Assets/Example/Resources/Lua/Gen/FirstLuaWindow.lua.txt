-- 此文件由 YouBindCollector 生成一次，后续不会被覆盖，可自由修改业务逻辑。

--- @param commonView CommonLuaView  挂载在预制体上的 CommonLuaView 组件
function FirstLuaWindow_Init(commonView)
    --- @type FirstLuaWindowView
    local view = FirstLuaWindow_InitializeView(commonView)

    -- 测试
    print("[FirstLuaWindow] Init done.")
    if view ~= nil then
        for key, value in pairs(view) do
            print("[FirstLuaWindow] " .. key, value)
        end
    end

    view.Text_Text.text = "Hello YouBind!"
    return view
end
