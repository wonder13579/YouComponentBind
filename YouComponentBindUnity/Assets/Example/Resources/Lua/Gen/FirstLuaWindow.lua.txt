-- 此文件由 YouBindCollector 生成一次，后续不会被覆盖，可自由修改业务逻辑。
--- @type FirstLuaWindow
local FirstLuaWindow = UIRegistry.Get(UIPanelName.FirstLuaWindowPanelName)

if FirstLuaWindow == nil then
    error("[FirstLuaWindow] Panel not found in UIRegistry. Please check g file registration.")
end

function FirstLuaWindow:Start()
    print("[FirstLuaWindow] Start")
    -- 测试用，先别删
    if self.view ~= nil then
        for key, value in pairs(self.view) do
            print("[FirstLuaWindow] " .. key, value)
        end
    end
    self.view.Text_Text.text = "Hello YouBind!"
end

function FirstLuaWindow:OnEnable()
    print("[FirstLuaWindow] OnEnable")
end

function FirstLuaWindow:OnDisable()
    print("[FirstLuaWindow] OnDisable")
    -- 预留：面板关闭时的清理逻辑
end

--- Button_Button.onClick
function FirstLuaWindow:OnButton_ButtonClick(...)
    print("[FirstLuaWindow] Button_Button clicked.")
end

--- Toggle_Item.onValueChanged
function FirstLuaWindow:OnToggle_ItemValueChanged(...)
    print("[FirstLuaWindow] Toggle_Item value changed.")
end

--- Toggle_Toggle.onValueChanged
function FirstLuaWindow:OnToggle_ToggleValueChanged(...)
    print("[FirstLuaWindow] Toggle_Toggle value changed.")
end
